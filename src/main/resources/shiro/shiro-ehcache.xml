<?xml version="1.1" encoding="UTF-8"?>
<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="http://ehcache.org/ehcache.xsd" name="shirocache">
    <!--指定缓存中的数据满了的时候在硬盘上的存储目录
        user.home:当前用户的主目录（例如我在window上登录的用户为lizhihui，这个目录就是C:\Users\lizhihui）;
        user.dir:用户当前的工作目录;
        java.io.tmpdir:Java运行的临时目录
        也可以自己指定一个目录
    -->
    <diskStore path="java.io.tmpdir"/>

    <!--默认缓存配置
        name:cache的标识符，在一个CacheManager中必须唯一
        maxElementsInMemory:缓存在内存中最大为多少
        eternal:设定缓存的elements是否有有效期。如果为true，timeouts属性被忽略
        overflowToDisk:设定当内存缓存溢出的时候是否将过期的element缓存到磁盘上

        以下属性是可选的：
        timeToIdleSeconds:缓存element在过期前的空闲时间
        timeToLiveSeconds:缓存element的有效生命期
        diskPersistent:在VM重启的时候是否持久化磁盘缓存，默认是false
        diskExpiryThreadIntervalSeconds:磁盘缓存的清理线程运行间隔，默认是120秒

        memoryStoreEvictionPolicy:当内存缓存达到最大，有新的element加入的时候，移除缓存中element的策略,
        默认是LRU(Least Recently Used，把最近最少使用的移除)，可选的有LFU(Least Frequently Used把一段时间内最少访问次的移除)和FIFO

        cacheEventListenerFactory：注册相应的的缓存监听类，用于处理缓存事件，如put,remove,update,和expire
        bootstrapCacheLoaderFactory:指定相应的BootstrapCacheLoader，用于在初始化缓存，以及自动设置。
    -->
    <defaultCache
            maxElementsInMemory="100000"
            eternal="true"
            overflowToDisk="true"
            timeToIdleSeconds="1800"
            timeToLiveSeconds="0"/>


    <!-- 密码重试次数 -->
    <cache name="passwordRetryCache"
           maxElementsInMemory="2000"
           eternal="true"
           timeToIdleSeconds="300"
           timeToLiveSeconds="0"
           overflowToDisk="false">
    </cache>
    <!-- 授权缓存 -->
    <cache name="authorizationCache"
           maxElementsInMemory="2000"
           eternal="false"
           timeToIdleSeconds="1800"
           timeToLiveSeconds="0"
           overflowToDisk="false">
    </cache>

    <!-- 认证缓存 -->
    <cache name="authenticationCache"
           maxElementsInMemory="2000"
           eternal="false"
           timeToIdleSeconds="1800"
           timeToLiveSeconds="0"
           overflowToDisk="false">
    </cache>
    <!-- session缓存 -->
    <cache name="shiro-activeSessionCache"
           maxElementsInMemory="2000"
           eternal="false"
           timeToIdleSeconds="1800"
           timeToLiveSeconds="0"
           overflowToDisk="false">
    </cache>
</ehcache>